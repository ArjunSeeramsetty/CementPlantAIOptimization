Implement the requested feature while strictly following all active workspace rules.

Execution requirements:
- First inspect the existing code structure and follow current patterns.
- Plan before making changes.
- Keep the implementation modular.
- Keep entry points thin.
- Add docstrings to all new public functions, methods, classes, and modules.
- Use logging only where operationally useful.
- Do not create unnecessary files.
- If temporary files are needed during implementation, delete them before finishing.

Before completion:
- Review the diff for duplicate logic, unused imports, dead code, and unnecessary files.
- Run relevant formatting, linting, and tests if available.
- Summarize changed files and confirm cleanup is complete.
